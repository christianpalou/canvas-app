import { FilterCardsBagPipe } from './filter-cards-bag.pipe';

describe('FilterCardsBagPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCardsBagPipe();
    expect(pipe).toBeTruthy();
  });
});
